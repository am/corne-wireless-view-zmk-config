# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano
#     shield: reviung41
#
# Bump the number below to invalidate the Actions cache (comment only, not in matrix)
# cache: 2
---
# Zephyr 4.1 / ZMK main: nice_nano_v2 → nice_nano (v2 is default revision)
include:
  - board: nice_nano
    shield: corne_left nice_view_adapter nice_view_gem
  - board: nice_nano
    shield: corne_right nice_view_adapter nice_view_gem
